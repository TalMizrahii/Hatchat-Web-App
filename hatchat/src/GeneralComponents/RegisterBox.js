function RegisterBox({children}) {
    return (
        <div className="register">
            <h1 id="chatName">
                Hat Chat
            </h1>
            {children}
        </div>
    );
}

export default RegisterBox;
